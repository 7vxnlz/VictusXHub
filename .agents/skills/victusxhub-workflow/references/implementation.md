# Implementation

Instructions → current state → exact search → selected files → smallest patch → focused verification → scoped diff → durable-state update only if truth changed.

- The nearest `AGENTS.md` applies; use one context pack only when useful.
- Search before reading. Do not read whole repositories or directories.
- Prefer compact wrappers by default. Preserve unrelated user changes and keep the diff tightly scoped.
