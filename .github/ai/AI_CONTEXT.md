# Legacy AI Context Pointer

Compatibility only; do not load by default. Use injected `AGENTS.md`, canonical `.github/ai/AI_WORKING_STATE.md`, the nearest scoped `AGENTS.md`, and a domain route from `.github/ai/CONTEXT_RECIPES.md` only when useful. Historical evidence is demand-loaded from `docs/`.
