# Legacy AI Context Pointer

This compatibility file is intentionally not part of the default read order. Its former architecture/status snapshot became stale after the inherited app import and HP work.

Use:

- `AGENTS.md` for global rules and routing
- `AI_WORKING_STATE.md` for current truth
- the nearest scoped `AGENTS.md` for local safety constraints
- one file under `docs/context-packs/` for task selection
- historical documents under `docs/` only when the active decision requires them
